# การติดตั้งและใช้งาน Notification Groups

## 🚀 การติดตั้ง

### 1. Migration และ Model
```bash
# ตารางฐานข้อมูลถูกสร้างแล้วตาม migration ที่มี:
# - notification_groups
# - notification_group_users (pivot table)

# ตรวจสอบว่า migration ทำงานแล้ว
php artisan migrate:status
```

### 2. Seeder สำหรับข้อมูลเริ่มต้น
```bash
# สร้างกลุ่มเริ่มต้น
php artisan db:seed --class=DefaultGroupSeeder

# สร้างสิทธิ์การใช้งาน
php artisan db:seed --class=GroupPermissionsSeeder
```

### 3. เพิ่ม Routes
```php
// เพิ่มใน routes/web.php
use App\Http\Controllers\Web\GroupController;

Route::middleware(['auth'])->group(function () {
    Route::resource('groups', GroupController::class);
    Route::post('/groups/{group}/add-user', [GroupController::class, 'addUser'])->name('groups.add-user');
    Route::post('/groups/{group}/remove-user', [GroupController::class, 'removeUser'])->name('groups.remove-user');
    Route::post('/groups/{group}/sync', [GroupController::class, 'syncMembers'])->name('groups.sync');
    Route::post('/groups/bulk-sync', [GroupController::class, 'bulkSync'])->name('groups.bulk-sync');
    Route::get('/groups/{group}/export', [GroupController::class, 'exportMembers'])->name('groups.export');
    Route::get('/groups/users', [GroupController::class, 'getUsers'])->name('groups.users');
});

// เพิ่มใน routes/api.php
Route::middleware(['auth'])->group(function () {
    Route::get('/groups/stats', [GroupController::class, 'getStats']);
    Route::post('/groups/preview-members', [GroupController::class, 'previewMembers']);
});
```

### 4. อัปเดต Navigation
เพิ่มเมนูจัดการกลุ่มใน `admin-nav.blade.php`:
```php
<li class="nav-item">
    <a class="nav-link {{ request()->routeIs('groups*') ? 'active' : '' }}"
        href="{{ route('groups.index') }}">
        <i class="bi bi-people-fill"></i>
        <span class="nav-text">จัดการกลุ่มการแจ้งเตือน</span>
        <span class="badge bg-light text-dark ms-auto" data-stat="active_groups">
            {{ \App\Models\NotificationGroup::where('is_active', true)->count() }}
        </span>
    </a>
</li>
```

## 🔧 การกำหนดค่า

### 1. สิทธิ์การใช้งาน
ระบบมีสิทธิ์ดังนี้:
- `view-groups` - ดูรายการกลุ่ม
- `create-groups` - สร้างกลุ่มใหม่
- `edit-groups` - แก้ไขกลุ่ม
- `delete-groups` - ลบกลุ่ม
- `sync-groups` - ซิงค์สมาชิกจาก LDAP
- `export-groups` - Export ข้อมูล
- `manage-group-members` - จัดการสมาชิก

### 2. Cron Job สำหรับ Auto Sync
```bash
# เพิ่มใน crontab
* * * * * cd /path/to/your/project && php artisan schedule:run >> /dev/null 2>&1

# หรือรันแบบ manual
php artisan groups:sync --type=department
php artisan groups:sync --group=1 --dry-run
```

## 📚 การใช้งาน

### 1. ประเภทกลุ่มทั้ง 5 แบบ

#### 🎯 Manual (กำหนดเอง)
- เลือกสมาชิกด้วยตนเอง
- เพิ่ม/ลบสมาชิกได้ตลอดเวลา
- เหมาะสำหรับกลุ่มพิเศษ

#### 🏢 Department (แผนก)
- สมาชิกจากแผนกใน LDAP อัตโนมัติ
- ซิงค์ทุก 6 ชั่วโมง
- เหมาะสำหรับแจ้งเตือนตามแผนก

#### 👔 Role (ตำแหน่ง)
- สมาชิกตามตำแหน่งงาน
- ค้นหาแบบ LIKE search
- เหมาะสำหรับผู้บริหาร/ผู้จัดการ

#### 🔗 LDAP Group
- สมาชิกจากกลุ่ม LDAP โดยตรง
- ซิงค์กับ LDAP อัตโนมัติ
- เหมาะสำหรับกลุ่มที่มีใน AD แล้ว

#### ⚡ Dynamic (เงื่อนไขกำหนดเอง)
- รวมหลายเงื่อนไข (แผนก + ตำแหน่ง)
- ยืดหยุ่นที่สุด
- เหมาะสำหรับกลุ่มซับซ้อน

### 2. ฟีเจอร์หลัก

#### 📊 Dashboard และสถิติ
- จำนวนกลุ่มแต่ละประเภท
- จำนวนสมาชิกรวม
- สถิติการแจ้งเตือน

#### 🔍 ค้นหาและกรอง
- ค้นหาตามชื่อกลุ่ม
- กรองตามประเภท
- กรองตามสถานะ

#### 👥 จัดการสมาชิก
- เพิ่ม/ลบสมาชิกแบบ Real-time
- ค้นหาผู้ใช้แบบ AJAX
- แสดงสถานะสมาชิก

#### 🔄 Auto Sync
- ซิงค์อัตโนมัติทุก 6 ชั่วโมง
- Manual sync ได้ทันที
- Bulk sync ทุกกลุ่ม

#### 📤 Export Data
- Export รายชื่อสมาชิกเป็น CSV
- รวมข้อมูลการเข้าร่วม
- รองรับ UTF-8

### 3. API สำหรับระบบภายนอก

#### ส่งการแจ้งเตือนไปยังกลุ่ม
```json
POST /api/v1/notifications/send
{
    "template_id": "urgent-alert",
    "recipients": ["team-leads", "IT-department"],
    "channels": ["teams", "email"],
    "data": {
        "title": "System Alert",
        "message": "Database maintenance scheduled"
    }
}
```

#### ดึงรายการกลุ่ม
```json
GET /api/v1/groups
Response:
{
    "success": true,
    "groups": [
        {
            "id": 1,
            "name": "ผู้ดูแลระบบ",
            "type": "role", 
            "member_count": 5,
            "is_active": true
        }
    ]
}
```

## 🔧 การแก้ไขปัญหา

### 1. LDAP Connection Issues
```bash
# ตรวจสอบการเชื่อมต่อ LDAP
php artisan ldap:test-connection

# ซิงค์ผู้ใช้จาก LDAP
php artisan ldap:sync-users
```

### 2. Performance Issues
```bash
# Clear cache
php artisan cache:clear
php artisan config:clear

# Optimize
php artisan optimize
php artisan view:cache
```

### 3. Debug Group Sync
```bash
# Dry run เพื่อดูว่าจะเปลี่ยนอะไรบ้าง
php artisan groups:sync --dry-run

# ซิงค์เฉพาะกลุ่มที่มีปัญหา
php artisan groups:sync --group=1

# ดู log
tail -f storage/logs/group-sync.log
```

## 📈 Best Practices

### 1. การตั้งชื่อกลุ่ม
- ใช้ชื่อที่สื่อความหมายชัดเจน
- ระบุประเภทในชื่อ เช่น "แผนก-IT" หรือ "ผู้บริหาร-ทั้งหมด"

### 2. การจัดการสมาชิก
- ใช้ Manual สำหรับกลุ่มพิเศษเท่านั้น
- ใช้ Dynamic สำหรับกลุ่มที่มีเงื่อนไขซับซ้อน
- ตรวจสอบผลการซิงค์เป็นประจำ

### 3. การ Monitor
- ตั้ง Alert สำหรับ Sync failures
- ตรวจสอบ Log เป็นประจำ
- Backup ข้อมูลกลุ่มเป็นประจำ

### 4. Security
- กำหนดสิทธิ์ให้เหมาะสม
- Audit การเปลี่ยนแปลงกลุ่ม
- รักษาความปลอดภัยของ LDAP credentials

## 🎯 สถานะการพัฒนา

### ✅ สำเร็จแล้ว
- [x] CRUD Operations สำหรับกลุ่ม
- [x] 5 ประเภทกลุ่มครบถ้วน
- [x] Auto Sync จาก LDAP
- [x] Web Interface สมบูรณ์
- [x] API สำหรับระบบภายนอก
- [x] Export ข้อมูล
- [x] Permission System
- [x] Command Line Tools

### 🔄 กำลังพัฒนา
- [ ] Real-time notifications
- [ ] Advanced reporting
- [ ] Group templates
- [ ] Approval workflow

### 📋 แผนอนาคต
- [ ] Group scheduling
- [ ] Integration with Teams การแจ้งเตือน
- [ ] Mobile app support
- [ ] Advanced analytics